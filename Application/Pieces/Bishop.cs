using Application.Core;

namespace Application.Pieces
{
    internal class Bishop
    {
        /*
        public override string Label => "B";

        public override IEnumerable<Position> GetMoves(Position currentPosition)
        {
            ;
        }
        */
    }
}
